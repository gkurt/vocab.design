// Authoring-round workflow TEMPLATE (two phases: Author x9, Verify x1).
// Fill the ALL-CAPS placeholders (ROUND number, BRIEFS path, CATEGORY_GUIDANCE),
// then run it with a Workflow-style orchestrator, or lift batchPrompt()/verifyPrompt
// into whatever agent harness is available: the prompts are the contract, the
// orchestration is fungible. Authors run on Opus-class models, in parallel, each
// owning one category; the verify agent runs once after ALL authors finish.

export const meta = {
  name: 'author-6-per-category-rROUND',
  description: 'Round ROUND: 6 terms per category (54); authors write and visually verify on the running dev server, verify agent runs static gates once',
  phases: [
    { title: 'Author', detail: '9 category agents, 6 terms each, visual pass on localhost:4321, no static checks', model: 'opus' },
    { title: 'Verify', detail: 'one agent runs validate + typecheck + biome once and fixes failures', model: 'opus' },
  ],
}

const REPO = 'FILL: absolute path to this checkout (agents cd nowhere, so a stale path sends them at nothing)'
const BRIEFS = 'FILL: absolute path to this round\u2019s briefs JSON'

// FILL: paste the ROSTER line the briefs generator prints. A workflow script has no
// filesystem access, so this literal is the only way the script can know what the round
// was SUPPOSED to produce, and knowing that is what lets the verify gate tell a
// half-finished round from a finished one. Round 25 is why it exists: an author agent died
// mid-response having written one term of a three-term ladder, its `completed` list was
// therefore empty, and the verify gate (told only what the surviving agents reported) found
// a published term contrasting two slugs that did not exist. It did the SPEC 2.3-sanctioned
// thing and minted them as STUBS, which is correct in isolation and exactly the debt the
// family was batched whole to avoid.
const ROSTER = {
  component: [],
  layout: [],
  pattern: [],
  interaction: [],
  motion: [],
  typography: [],
  color: [],
  aesthetic: [],
  accessibility: [],
}

const BATCH_SUMMARY = {
  type: 'object',
  properties: {
    completed: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
  required: ['completed', 'notes'],
}

const VERIFY_SUMMARY = {
  type: 'object',
  properties: {
    clean: { type: 'boolean' },
    fixed: { type: 'array', items: { type: 'string' } },
    // One line per roster slug that is absent or half-written, so an interrupted round
    // arrives as data rather than as a sentence inside `notes` that nobody greps for.
    incomplete: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
  required: ['clean', 'fixed', 'incomplete', 'notes'],
}

// The accumulated laws. NEVER trim these; append new ones as rounds teach them.
const STAGE_NEWS = 'Stage facts newer than most exemplars: (1) the player MIRRORS its pointer into data-hovered/data-pressed on whatever it hovers, clicks, or drags, claiming only attributes your handlers did not set, so never wire pointerenter/leave listeners merely to repaint hover, and never assert data-hovered/data-pressed absence on an element the cursor is resting on at that moment. (2) `type` steps land one character at a time, one input event each, at 70 ms cadence. (3) A small control whose artwork the cursor would cover may carry `data-aim` to park the ghost cursor at its bottom-right corner; events land there too, so never on an element that resolves input by coordinate. (4) During a drag the cursor becomes a grab hand and the source holds pressed paint. (5) A subject whose states include a counter-example it itself passes through declares the honest condition as a selector in data-pose on the subject (see dark-pattern/demo.ts or confirmshaming/demo.ts); identify refuses to pose a state that fails it, and the MOUNT state must satisfy it. (6) A site named in article prose must be a markdown link to that site, never a bare domain or raw URL; the validate gate rejects violations. (7) The stage canvas is 320px TALL and about 476px wide: match sibling demos (frames typically 440-476 wide, under ~310 tall), never build to a 320px width. (8) TIMING LAW: an assert after a click is judged roughly 350 ms after the click lands, so never assert a mid-flight state of a move shorter than ~500 ms, aim asserts mid-window never at a window edge, and never assert visibility of an element INSIDE a popup the asserted action just closed (mirror the evidence onto the trigger). (9) SELECTOR LAW: an unquoted attribute value in a choreography selector must be a valid CSS identifier, so quote anything starting with a digit ([data-age="8-min"], never [data-age=8-min]); the validate gate now rejects this. (10) HAIRLINE LAW: the stage treats boxes thinner than ~2px as absent, so never assert visibility of a hairline (a vertical SVG line at rest, a 1px rule): aim the claim at the container that carries it, and draw deliberate guide lines at 2px or more. But CHECK THAT THE CONTAINER HAS A BOX, because the obvious wrapper usually does not: a group whose children are all position:absolute, or one held at height:0 so guides can overlay a line without shifting it, measures zero and is read as absent exactly like the hairline was. Round 15 lost two specimens to this, both by following the first half of this law. Claim the drawn element itself once it is 2px or more, and if the state you need lives on the zero-height wrapper, move that attribute onto something with a real box rather than asserting the wrapper. (11) MOUNT-FADE LAW: kit surfaces fade in from mount (the scrim takes 220 ms), so a choreography opens with a wait before its first asserts rather than judging at t=0. (12) The identify-mid-attract e2e pass (motion on) will press Identify at an arbitrary script moment: the subject must be honest at EVERY resting state, not just at mount. (13) NO MANUFACTURED EVENTS: the takeover pass counts the clicks that reach a specimen and requires exactly ONE, so a demo may never answer input by synthesizing more (`someButton.click()` inside a click handler fails it, even though the choreography passes). Drive a kit primitive through its own property instead: `<sp-segmented>` exposes a `value` setter that moves the thumb and fires `change`, so cast the part (`part(root, "mode") as HTMLElement & { value: string }`) and assign, as chevron/demo.ts does. (14) STILL SCRIPTS: a term whose whole claim is visible at rest (most aesthetics, much typography, some layouts) ships a choreography of waits and asserts ONLY. Never write cursor theater: a hover with no visible consequence (no data-hovered styling, no handler, nothing revealed) demonstrates nothing; pointing at parts in sequence is the identify pin’s job, not the cursor’s. Before going still, check whether the term honestly has a second state the demo should show instead (SPEC section 8). (15) SUBJECT GRANULARITY: "the term is a thing this element HAS" is the wrong subject test; it justifies any container up to the page. When the term names a feature inside an element (a channel, a gap, a stroke, a band), the subject is the element tracing that feature: a demo that draws or highlights the feature marks the drawn overlay, and a feature with no element of its own is given one sized to its extent, never its container by proxy. Identify summons a subject the current state hides, so a sometimes-invisible overlay is a legal subject. (16) INVISIBLE AIM ANCHORS: an element that exists so the script can aim at a coordinate (a tap that must land inside an invisible region) gets a data-part and NO paint: no dashed ring, no dot, nothing. The ghost cursor is the only visible pointer artifact; drawn stop-point markers annotate the script, not the term, and read as UI the term never had. To teach where input can land, draw the region (the term’s own geometry) or say it in a caption. (17) TOUCH PERSONA: mark a touch-native surface (or the whole scene) with data-touch, and every step targeting it performs as touch: fingertip disc instead of the arrow, pointerType "touch" on events, and NO hover dispatched or mirrored, so never rely on data-hovered inside a touch scope. The `hold: <ms>` step presses the current target for that long; under the touch persona its pointermove events carry pressure climbing at a finger’s rate (full force at 900 ms), so the hold’s LENGTH chooses the depth (450 ms is a light press, 900+ bottoms out), and it ends with pointerup, NEVER a click (script the tap separately if the demo wants one). A pressure-driven demo wires pressureHold from #src/kit/touch.ts with its DemoClock: one force signal for the script, a finger, and a real reader holding a mouse button. A touch gesture demonstrated by clicking state pickers (tabs for pressure levels) is banned. The pinch exists (law 21), rotation rides it (law 26), and contact counts up to three exist (law 41). (18) REFUSE TO FAKE THE PLAYER: if a term’s honest demonstration needs input or stage machinery the player does not have (a gesture, a persona, a state the stage cannot hold), do NOT ship a simulation control or state picker in its place. Leave the term unauthored, return it in your notes as `needs stage primitive: <what>`, and move on; the primitive gets built first and the term comes back in a later round. A simulation control is legitimate ONLY for conditions no input could ever perform (a network failure, a server delay, a permission state). (19) HELD KEYS: `{ holdKey: { key, ms } }` holds a key with the OS repeat shape (keydown, ~500 ms typematic delay, then repeat:true keydowns every ~90 ms, keyup at release; the chip counts "Key ×N"), so key-repeat behaviour is performed, never simulated; demos read `event.repeat`. A held MODIFIER across other steps exists now (law 23). And a keyboard-driven demo must be drivable by a real keyboard: its control carries tabindex="0" so a reader can focus it and the real keys reach it (attract itself never moves real focus). (20) AIM CLEAR OF OVERLAYS: events dispatch on the moveTo’d ELEMENT, so a click "outside" a popover works even when the container’s centre sits under the popover, but the drawn cursor lands at that centre and reads as pressing the surface it is dismissing. Aim outside-clicks at a real element visibly clear of any open surface (a caption, a line of prose), never at a big container’s centre. Also: `.sp-icon` is display block, so an icon dropped bare into a non-flex button breaks its label onto a second line; state inline-flex + gap on the button. (21) PINCH: `{ pinch: { scale, ms? } }` spreads (scale > 1) or closes (scale < 1) twin touch contacts about the current target — two pointerdowns with distinct pointerIds, pointerType "touch", the separation ending at exactly `scale` times where it began; the ghost draws twin discs. `ms` is animation, not semantics (reduced motion collapses it — unlike hold, whose length IS the depth). A pinch-driven demo wires pinchSpread from #src/kit/touch.ts on its gesture surface, inside a data-touch scope: ONE scale signal for the script, a real two-finger pinch, and a reader’s mouse via Ctrl+drag (a virtual second contact mirrors the pointer, drawn by the stage; dragging down-right opens, back up-left closes). Anchor the response at the centre onStart reports: the content under the fingers stays under the fingers, which is usually the term’s whole claim. Trackpad pinch arrives as a ctrl+wheel event, not pointers, and is the demo’s own to wire beside it (see pinch-to-zoom/demo.ts for the full shape: anchored zoom math, pan clamped so the scene always covers its box). Rotation rides the same step (law 26). (22) CAPTURE REAL DRAGS: a demo that holds a drag (pointerdown, pointermove tracking, pointerup) must capture the pointer or a reader’s drag dies at the element’s edge — moves stop arriving and the release lands elsewhere. Put `if (event.isTrusted) el.setPointerCapture(event.pointerId)` in the pointerdown handler; the isTrusted guard is MANDATORY because the attract player’s synthetic pointers have no active pointer to capture and the call THROWS, killing the handler and the scripted drag with it. End the drag on pointerup AND pointercancel, never on pointerleave (boundary events do not fire while captured). This bug is invisible to every e2e pass — synthesized events dispatch directly on the element — so only the takeover hand-check catches it; write the capture line from the start. (23) HELD MODIFIER SCOPE: `{ withKey: { key, steps: [...] } }` holds a key across the enclosed steps — keydown as the scope opens, keyup as it closes, the HUD chip held for the duration. Shift, Control, Alt, and Meta stamp their flag on EVERY event dispatched inside (clicks, drags, keys: a click becomes Ctrl+click, a drag Shift+drag), scopes nest for chords, and the scope closes even on a cancelled run so a held key never leaks. Demos read the real flags (`event.ctrlKey || event.metaKey` — both, never pick a platform) and may listen for the modifier’s own keydown/keyup to light a legend chip (modifier-key/demo.ts is the shape). A simulated-modifier control (a segmented "Ctrl held" picker) is banned per law 18. (24) PATH DRAG: `{ drag: { to, via: [...] } }` runs one continuous press through via waypoints — a polyline, not several strokes — so gesture strokes, lassos, signatures, and marking-menu swipes are performed as ONE stroke. Waypoints are invisible aim anchors (law 16: data-part, no paint). The cursor and the dispatched moves trace the same polyline, one move per animation frame at a LINEAR pace (the release velocity a fling or swipe reads from the final deltas is real, so drags are never eased), landing exactly on each waypoint; under reduced motion a leg collapses to three quick beats with the vertices intact. A demo that resolves the stroke geometrically gets a polyline through the waypoints, not a curve. (25) WHEEL VS SCROLL: `{ wheel: { y, x?, ms? } }` dispatches a short burst of real WheelEvents at the current target (total delta split across ~5 events, the shape a notch or trackpad flick arrives in); `scroll` moves a scroller’s position directly and fires no wheel or pointer events of its own (assigning scrollTop still produces the element’s native scroll event, which is why a scroll-listener demo like scroll-pinning is driven correctly by it). A demo that LISTENS for wheel (a zoom surface, an overscroll edge, a wheel-adjusted control) is spoken to with `wheel`; a demo whose content merely needs to be somewhere else is scrolled with `scroll`. Modifier flags ride along, so a trackpad pinch (ctrl+wheel by browser convention) is a wheel inside a withKey Control scope — see pinch-to-zoom/choreography.ts. Event count survives reduced motion (a demo may count events); only the pacing collapses. Negative y wheels up / zooms in. (26) ROTATE: `{ pinch: { turn: <deg>, scale?, ms? } }` rotates the twin contacts by that many degrees, clockwise, about the current target — alone for a pure twist or combined with scale. pinchSpread reports (scale, turn) per move; a demo uses the half its term names and ignores the other (existing scale-only consumers are untouched by the second argument). Turn is RELATIVE per gesture: the demo adds it to the angle it had committed when the gesture engaged (rotate-gesture/demo.ts is the shape, including the ±limit clamp and the level detent applied to the live signal only). A mouse turns by holding Ctrl and SWINGING around the mirror centre, drawn by the stage; the demo draws no contacts of its own — the ghost’s twin discs and the reader’s mirrored pair are the fingers, so drawn fake fingers on the surface are now double-vision and banned. Contact counts up to three exist (law 41). (27) GAZE PERSONA: mark a gaze-native surface (spatial/visionOS-style UI) with data-gaze. Events stay EXACTLY a mouse’s — hover included, looking IS hovering — and only the dress changes: the ghost is an eye resting where the reader looks, and a click draws as the hand’s pinch closing instead of the arc. A demo in a gaze scope wires plain pointerenter/click handlers and nothing else; captions never apologize for simulation (the persona portrays the input as itself), and a reader’s own pointer plays the eyes in takeover — no mirror, no hidden cursor, so never wire cursor styles or data-touch tricks on a gaze surface. Gaze is designed for looking and committing (moveTo, click); gaze drags and holds are UNDESIGNED — a demo needing them is a refusal (law 18), not a mouse-dressed compromise. See look-and-pinch/demo.ts for the shape, including the data-pose that keeps identify honest about where the eyes rest. (28) EXIT-DIRECTION ASSERTS: law 8 says aim a mid-flight assert mid-window, and that advice only holds for a move that fades IN. `isSeen` (src/stage/visible.ts) treats opacity at or below 0.05 as invisible, so a visible claim on an element that is fading OUT fails however wide its window is: on a decelerating ease the opacity crosses that floor early, while the same curve inbound is nearly opaque at the same instant. Round 16 lost zoom-transition to exactly this, its opening `[data-state=moving]` claim passing and its closing one failing on the identical selector and duration. An exit is claimed through evidence that stays visible: a scrim losing its open flag, a restored source element, a readout that changed. Never through the departing element itself. (29) MATCH THE READOUT’S SEMANTICS: an assert has to mean what the element it selects means. A "last key"/"last event" readout after several inputs reflects only the FINAL one, so a cumulative claim aimed at it is wrong even when the demo is right: character-key-shortcuts typed "sorry" and asserted its log had fired, but the word ends in an unbound letter, so the log honestly read "no" while the cumulative star and reply states carried the damage. Worse than the failure was its mirror image in the same script, an assert that PASSED for a reason the policy under test never touched, and so would have passed under every policy. Aim cumulative claims at cumulative state, last-event claims at last-event readouts, and when an assert passes, check it could have failed. (30) ALIAS ROUTE HIJACK: an alias whose slugified form equals its own term’s slug shadows the term’s own page with the alias redirect (`half leading` claiming /half-leading/, `container transform` claiming /container-transform/; two terms hit it in round 16). The validate gate already catches it as a self-collision at scripts/validate-terms.ts, so it cannot ship, but it costs a debugging round: an alias exists to catch a DIFFERENT spelling, so reword it or drop it. (31) GEOMETRY IS PART OF THE CLAIM: the stage body clips its overflow, so an element that escapes it is silently amputated, and a container that cannot hold its content spills onto neighbours or cuts itself. At EVERY state the choreography visits: nothing visible leaves the stage body, no container spills or cuts its content (unless the clipping viewport or the truncation is itself the design), nothing overlaps a neighbour it does not mean to, and a single-line control (.sp-button, .sp-chip, .sp-tab) holds ONE line — white-space nowrap plus flex 0 0 auto, with room sized for the WIDEST state, measured once on mount when only runtime knows it. A geometry auditor (the sweep skill’s stage-geometry detector) samples every specimen through a full play and flags escapes, spills, folded controls, overlaps, and incidental shifts; author to pass it, sizing boxes at real rendered type rather than at whatever state was on screen while authoring. (32) HOVER SWEEPS: the ghost cursor streams interpolated pointermove events while it travels between hover targets (one per animation frame, the coalesced rate a real mouse arrives at; buttons: 0 so a sweep never reads as a drag, eased to track the drawn cursor), dispatched onto the deepest element containing both endpoints so container listeners hear them. A demo whose term is CONTINUOUS pointer response (dock magnification, proximity glows, cursor-tracking paint) therefore listens for pointermove on its container and computes from clientX/clientY, and the attract pass will genuinely sweep it; discrete enter/leave wiring stays right for discrete terms (tooltips, menus). Hover enter/leave still land only at the endpoints, no sweep exists under the touch persona or reduced motion, and a pointermove listener that should only act mid-press must gate on its own pointerdown state, never on receiving a move. (33) HOVER-DRIVEN SURFACES: a demo operated by hovering alone (a dock that bulges, a proximity glow, a spotlight that follows the pointer) marks that surface data-hover-driven, so a reader’s 150 ms dwell there takes the stage over without a click — the term’s honest operation is the reader’s own pointer, not a spectator’s view of the ghost’s. data-gaze scopes are hover-driven implicitly (looking IS hovering). Mark only surfaces where hovering alone is the interaction: a demo where hover merely decorates a click (a button glow, a tooltip on a control) keeps the default, so a pointer passing across it never hijacks the show. (34) ONE RUN OWNER: an animation run has one owner at a time, and a script never cuts a run the reader can see. If the demo autoplays a run on mount, the script presses Replay only AT REST (open with a wait that outlasts the mount run: duration + transition lead + a beat) — a replay clicked mid-run teleports the movers to zero under the reader’s eye. Alternatively skip the mount autoplay and let the scripted Replay name the only run, with mount an honest rest state. The script’s tail outlasts every run it starts, so the loop’s remount cuts at a landed state, never mid-flight; and a demo that re-arms its run on its own clock does not also get scripted Replays — pick one owner. (35) LOOP PERSISTENCE: the attract loop remounts the demo between iterations (mount is the only universal reset), with two exceptions that keep the tree so devtools inspection survives and ambient animation runs unbroken: a wait/assert-only script on a demo that never armed its clock persists automatically, and a demo whose pass provably ends at its mount state may declare data-loop="keep" on its root — verified, not trusted: the smoke test plays a declaring demo’s script TWICE with no remount between, and a dirty second lap fails with a "second lap" marker. Never declare it on a demo that self-animates on its clock (phase-locked to mount: a still script’s waits assume the cycle starts there — tabular-figures is the canonical refusal) or whose script leaves any counter, selection, or revealed surface changed. The ghost sends its trailing leave at each pass boundary, so symmetric hover state settles on its own. (36) TAP AND SCRUB: the contacts do two more things besides spreading. `{ tap: { fingers, count } }` taps them on the current target with no travel (count times: a magic tap is TWO of two, and one tap is deliberately not the gesture), and `{ scrub: { fingers, reps, ms } }` sweeps them sideways and back under one continuous press (the z the platform gesture is described by; ms is animation, so reduced motion collapses it). A demo wires contactTap or contactScrub from #src/kit/touch.ts inside a data-touch scope: one signal for the script, real fingers, and a reader’s mouse. The reader’s mapping is the SAME modifier press the pinch uses, split by travel: a modifier+tap is the tap, a modifier+drag is the pinch, and a modifier+drag swept side to side is the scrub, so ONE ELEMENT NEVER WIRES A TAP AND A PINCH TOGETHER. TouchMirror already draws the twin disc for any modifier+press, so nothing in the stage needs touching to support a reader. (37) PORTRAY, THEN CAVEAT IN PROSE: magic-tap and escape-gesture exist because a gesture the WEB CANNOT HEAR is still portrayable. VoiceOver consumes the two-finger double tap and the two-finger scrub natively: neither emulates a keystroke, neither delivers its contacts to the document, and dblclick does not fire under VoiceOver at all, so no page can listen for either. That is a fact for the ARTICLE, not a licence to dress the gesture as something else: wiring keydown Escape and calling it the scrub is exactly the fake law 18 forbids, because it teaches a handler that cannot work. Portray the gesture as the contacts it is made of, and let the prose carry what the demo cannot say. The corollary is the harder half: a choreography can be GREEN while the demo lies. escape-gesture first passed while translating its dismissed sheet outside a clipping frame and asserting it visible, which is clipped rather than gone; the fix was to hide the sheet and move the claim onto the screen that stays (laws 28 and 29 together). Prove a new primitive with audit() BEFORE authoring on top of it. (38) IMPORT THE KIT ELEMENT YOU USE: a kit custom element only upgrades in a document that has imported its module, so a demo whose markup contains <sp-segmented> or <sp-combobox> must itself `import \'#src/kit/segmented.ts\'` (or combobox.ts). Miss it and the tag renders as an inert unknown element: it LOOKS right, the choreography presses a segment, the thumb does not move, and the asserts fail with nothing to point at. juiciness lost three asserts to exactly this. Now gated by scripts/validate-terms.ts, which strips comments first so naming an element in prose is not using one. (39) SWEEP LESSONS (from the first consolidated specimen sweep, 2026-08-22; the full findings live in .claude/skills/specimen-sweep/COMPLAINTS.md). PLAYER STEP COSTS ARE REAL TIME: CURSOR_TRAVEL_MS is 550 and STEP_GAP_MS is 350, so a moveTo costs 900 ms and a click 350 ms more, and an opening `wait: 600` therefore fires its click at ~1250 ms, which is exactly how a 1.6 s mount run collides with a script that looked comfortably clear. What an opening wait must beat is the demo settle timeout (lead + duration + settling beat), never the animation duration alone, and a 60 to 70 ms lead often hides inside a transition shorthand. Read "a run" as anything scheduled on the clock that has not reached its terminal state, cue chains and auto-advancing cards included; for a self-advancing demo a tail of dwell + a beat is always safe and needs no phase measurement. CAPTURE THE PRESSED ELEMENT, not the root, even when pointermove and pointerup listen on root: captured events retarget to the capture target and still bubble, so the root listener keeps firing, while capturing root itself swallows presses that were never drags. The line goes at the TOP of the handler when early returns decide WHICH thing was grabbed, but AFTER the guard that decides whether a drag starts at all. Any flag cleared only by pointerup needs the capture too, whether or not position is the payload. data-hover-driven IS INERT ON A CONTROL, because the stage dwell already fires on a[href], button, input, select, textarea and [tabindex]: mark only non-interactive surfaces (a scene, a field, a card, a bare span), and know that the attribute lands inside the recorded subject selector, so marking a surface that IS the subject rewrites its snapshot. AN INVISIBLE AIM ANCHOR TAKES ITS CAPTION WITH IT: copy that named the paint has to name the term own geometry instead of the pointer itinerary, the anchor keeps its CENTRE and not merely its offsets, and it never sits in .sp-context, which would claim paint that no longer exists. To tell a marker from the term own geometry, ask whether any choreography step names it: every real offender is a moveTo target. AN ORPHANED HIGHLIGHT IS NOT A MISPLACED SUBJECT: accent on PEER INSTANCES of the term is the comparison, so keep the subject on one instance rather than climbing to the container holding them all (a legend swatch must keep the bar colour or it stops being a legend). What convicts is paint the demo uses to DRAW the feature while the ring sits on the thing carrying it, so if the specimen draws the property as a box, ring the drawing. When a subject moves onto a state-dependent element its data-pose moves with it, written to the new subject in the same pass via flag(el, "data-subject", ...) beside the paint rather than as literal markup. (40) STILL SCRIPTS AND LOOP PERSISTENCE, sharpened by the second sweep pass (2026-08-22). Kit hover paint EXISTING does not make a hover legitimate: the test is whether that paint is the TERM OWN CLAIM, because a generic affordance (a table row tint, a button brightening) lights up under the ghost while demonstrating nothing. The exemption is per-ELEMENT, not per-script, so a tour of six static parts does not earn its keep because one stop happens to be a button. These kit classes have visible [data-hovered] paint: sp-button (with --ghost and --quiet), sp-icon-button, sp-table tbody tr, sp-day, sp-chip-remove, sp-nav-item, sp-menu-item. These have NONE: sp-checkbox, sp-switch, sp-chip, sp-segment, and every text class. When a legitimate hover is kept, assert the MIRRORED STATE ATTRIBUTE rather than bare visibility, so the script text itself shows the hover has a consequence. When stilling a tour, PROMOTE each toured part to an assert instead of deleting the step, and keep the wait that followed: deleting alone silently drops coverage the hover was incidentally providing, and the promotion often finds an unasserted element that IS the claim. The offender own comment is usually the tell, some variant of "a poster answers no pointer, so the cursor reads it motif by motif". FOR data-loop="keep": the pass-boundary leave is NOT a leave for the container, because the player dispatches pointerleave on the LAST HOVERED ELEMENT only and enter/leave do not bubble. A demo that resets on a container pointerleave while its script parks the ghost on an aim mark inside that container never gets its reset, so the test is not whether the reset is symmetric but whether the element the ghost stands on when the pass ends OWNS the reset. The data-hover-driven field pattern therefore ends mid-response by construction and keeps remounting. And a green second lap is not proof: a completed dwell or a changed readout passes every assert twice while plainly not being the mount state, which is why the rule names counters, readouts and revealed surfaces. (41) CONTACT COUNTS UP TO THREE: `tap`, `scrub` and `pinch` each carry a `fingers` count, two by default and THREE AT MOST. The contacts sit evenly along the gesture axis, so the outermost pair always carries the stated scale and an odd third rides the centre, where the ghost draws its third disc. A count is never one (a single touch tap is a `click` inside a data-touch scope) and never four: no term needs it, so a demo that would waits, exactly as three did. The kit side is contactTap and contactScrub (the old twoFingerTap and twoFingerScrub are gone, not aliased), plus contactCount for a term whose claim IS the count rather than any one named gesture: read the count directly instead of inferring it from a gesture you did not ask for. A reader on a mouse stands in for the contacts by holding MODIFIERS, and it is HOW MANY that counts, not which: any one modifier is a pair, any two is three, more still asks for three. Ctrl, Alt, Shift and Meta are interchangeable (a mouse cannot put a second finger down, so no key is anointed), and `readerContacts` in #src/kit/touch.ts is the one definition the drawn discs and every handler share. Count the modifiers with Boolean coercion, never Number(): an event built with no modifier fields counts NaN and reads as a gesture, which cost a test. Where the platform swallows the gesture before any document sees it, pass `reader: false` so no mouse mapping is offered, because handing a reader a mapping for a handler that cannot work on real hardware is the fake law 18 forbids (law 37 is the same principle: portray the contacts, caveat in prose). NEVER paint your own fingers: the stage draws every contact, for the script and for the reader alike, so drawn dots are double vision. Two traps the build hit. A gesture signal is RELATIVE to where the gesture engaged, so anchor the response on the value committed at onStart and never compound each move into the last, or a spread runs straight to its ceiling. And pinchSpread deliberately holds the FIRST TWO contacts, so with three down the scale it reports is still right (a spread is a uniform scaling, so every pair shares one ratio) while the centre it reports is the midpoint of those two rather than of all three: anchor on that point only for a pair, and read contactCount when the count itself is the claim. And validate now rejects a `fingers` outside 2 to 3, plus any of these steps in a demo with no data-touch scope. DEVICE MOTION IS NOT AN INPUT: a shake is read from a sensor rather than made by a pointer or a key, so it belongs to the environment carve-out beside a network failure or a permission state, and a labelled control driving it is legitimate. The test is what the input IS, not whether the stage could synthesize an event for it. (42) A DRAG SAYS HOW IT LETS GO: `drag` now carries `release` and `ms`. The default `release: rest` settles for a beat at the destination before lifting, which is a hand that stopped before it let go, so a demo judging the last samples of the stroke honestly measures no speed. `release: moving` lifts the instant the travel ends, which is a throw, and `ms` is how long that travel takes: distance over time IS the release speed. A thrown drag MUST state its ms (validate rejects one that does not, and holds it to 80-1200 ms), because a throw the player makes too slowly is one a recognizer correctly reads as a hand at rest. The travel is identical either way, so a term showing both scripts one step each and lets its own recognizer judge them rather than asserting either state into being. fling was rewritten as the first consumer, its Simulate-a-throw button deleted: a 90 px stroke in 200 ms measures 440 px/s and carries 139 px, while the settled stroke beside it still stops with the hand. Watch the window a demo judges on: one that filters its samples by the clock AT RELEASE reads the settle honestly, while one that prunes them as they arrive keeps pre-settle samples and coasts either way. (43) TWO MORE STAGE FACTS. HOVER INSIDE A TOUCH SCOPE COMES ONLY FROM A TAP, AND THEN IT STICKS. No hover ever arrives from travel, for the script or for a reader: every :hover rule in the kit is guarded with :not([data-touch], [data-touch] *), so inside a touch scope hover paint is attribute-only, while :active stays unguarded because a finger really does press. What a real device leaves behind is a hover STRANDED by a tap, so the stage lands data-hovered on the tapped element and leaves it until a tap elsewhere (src/stage/touch-hover.ts), claiming only what your handlers did not set. So NEVER wire pointerenter inside a touch scope to repaint hover: that hands a reader the one thing a finger cannot do. sticky-hover had exactly that wiring and lost it. A multi-contact tap is a gesture, not a place a hover can rest, and strands nothing. SECOND: a drag can DWELL at a waypoint. A via entry written as { at, dwell } stops there for that many ms and dispatches NOTHING while it waits, because a pointer holding still is what emits no events, and that pause is what a spring-loaded container or any drag-and-dwell target listens for on its own clock. Semantics, not tempo, so reduced motion collapses the travel around it and never the pause. spring-loading was the first consumer: it had been springing on a BARE hover with nothing carried, which fabricated the term, and the lesson generalizes past the fix. When a demo cannot perform its real input it tends to widen a trigger until something reachable sets the state, and the widened trigger then reads as the term. Ask what the term REQUIRES (spring loading requires a carried item) and check the demo cannot reach the state without it. (44) A KEY IS TAKEOVER, AND A HELD KEY MUST KEEP IT. Every trusted keydown inside a specimen claims the stage, not just the first, and the pointer leaving no longer hands the stage back while focus is still inside. Without both, a reader holding a key had attract resume underneath them and the scripted keyup landed in the demo and ended their hold: quasimode flickered on and off once per pass, six times in thirty seconds, measured. Keys that do not auto-repeat (Shift, Control) are why the second half matters, since there is no later keydown to re-claim the stage with. Two things follow for demos. A keyboard demo needs tabindex so a reader can reach it AND it must refuse the default of every key it claims, or the reader drives the demo and scrolls the page at once (space, arrows, Home, End, PageUp, PageDown all scroll); refuse per key so Tab still moves focus, and never refuse a key whose default IS the demonstration (space activating a real button is the whole of first-rule-of-aria). Synthesized keys scroll nothing and are untrusted, so BOTH defects are invisible to the choreography and to CI: only a real keyboard finds them. (45) A DRAG CAN HOLD THE RIGHT BUTTON: `drag: { to, button: \'right\' }` keeps button 2 down for the whole stroke, every event reporting it, the ghost drawing its right arc, and the release followed by the contextmenu a real right button fires, so a pad that must refuse the menu is actually asked to. It stays a mouse gesture inside a touch scope, exactly as rightClick does; middle waits until a term needs it. Written because mouse-gesture had LATCHED instead: its pad armed on a rightClick and read the stroke on the next one, so the scripted stroke was drawn with the LEFT button while an on-screen caption explained the ghost cursor\'s limitation to the reader. That is the shape to watch for, and the rule is unchanged (SPEC §8): input the player cannot perform is a reason to grow the player, never to invent a control or a latch that stands in for the input, and never to explain the apparatus in copy the reader sees.'

const VISUAL_PASS = 'VISUAL PASS: the user is already running the dev server at http://localhost:4321 (confirm with a GET request before relying on it; Astro dev answers HEAD with nothing, so a HEAD probe reads as "server down"). After authoring each demo, load http://localhost:4321/<slug> with NO trailing slash (astro.config sets trailingSlash: never, so a trailing slash is the 404, not the fix; retry once on a transient miss right after an edit, HMR can race, and check response.ok() so a bad URL can never masquerade as a finding) headlessly and measure: nothing overflows the stage body or its own frame, no element escapes its container, reserved slots hold when you flip states, captions have room at their real rendered size. KNOWN QUIRK (also in AGENTS.md): bun cannot launch Playwright chromium on this machine, so run your probe script under node, importing chromium from @playwright/test. Do NOT start, stop, or restart any dev server or preview; do NOT run bun dev/build/test/validate/typecheck or any other repo script; if localhost:4321 is ever down, note it in your summary and continue WITHOUT the visual pass rather than launching anything. Never leave probe scripts or screenshots at the repo root.'

const AUDIT_PASS = 'AUDIT PASS (mandatory, once per specimen, before you move on to the next term): the stage exposes its own smoke test, so PLAY your choreography through the real attract player instead of reasoning about whether it holds. `document.querySelector("vd-stage").audit()` mounts the demo fresh, runs the script once with motion on, and resolves { failures: [{ step, selector, expected }], interrupted, subjects }. A term is not finished until failures is EMPTY, subjects is EXACTLY 1, and interrupted is false. That triple is verbatim what e2e/choreography.e2e.ts asserts (see src/stage/specimen-stage.ts audit() and src/stage/player.ts audit()), which is why a specimen you audit clean almost never fails the gate and one you skip is a coin flip. Drive it from the same node probe script as the visual pass: goto http://localhost:4321/<slug>/, `await page.waitForFunction(() => !!document.querySelector("vd-stage").specimenRoot)`, `await page.evaluate(() => document.fonts.ready)`, then `await page.locator("vd-stage").evaluate((el) => el.audit())`. Inline that evaluate: never import anything from e2e/, a rule that covers your probe script as well as your demo, and never leave the probe or its screenshots in the repo. Reload the page between audits, because audit() leaves the player in user mode and owns the demo until resume(). Read a failure report literally: `step N: <selector> should be <state>` names the exact assert, and the usual causes are an assert timed to the edge of a transition, a claim on evidence inside a popup the step just closed, a claim on an element that is fading OUT (law 28), and a wrapper with no box (law 10). ONE HONEST CAVEAT: this runs against the DEV server, which injects CSS through JS, so a specimen can mount before the stage has its height while the real gate on 4322 grades the built page. Waiting on specimenRoot and fonts.ready closes most of that gap, so treat a clean audit as a strong pre-check rather than a guarantee: a failure here is always real, a pass here is merely very likely.'

// FILL: one entry per category. Anatomy of good guidance: 3-6 exemplar demos by
// path (including LAST round's siblings of this round's terms), every IOU ("term X
// on site NAMED your term Y: link back"), every alias fence the collision check
// found ("do not claim Z, it belongs to W"), and any category-specific discipline
// (aesthetic: static poster tours, inset, inline paint; accessibility:
// data-sim-focus always; interaction: labeled-sim + real-wiring for gestures the
// player cannot make). The kit is FROZEN in every category.
const CATEGORY_GUIDANCE = {
  component: 'FILL',
  layout: 'FILL',
  pattern: 'FILL',
  interaction: 'FILL',
  motion: 'FILL',
  typography: 'FILL',
  color: 'FILL',
  aesthetic: 'FILL',
  accessibility: 'FILL',
}

function batchPrompt(category) {
  return [
    `You are authoring ${ROSTER[category].length} term(s) for vocab.design (stage 3 of SPEC section 11), in ${REPO}. Category: ${category}. They are, exactly and only: ${ROSTER[category].join(', ')}. Your briefs are under the "${category}" key in the JSON at ${BRIEFS} (definition, useWhen, aliases, pre-filtered implementations, sources, demo hint per term); if that key does not hold exactly those slugs, stop and say so rather than guessing which list is right.`,
    '',
    'Read ONCE before the first term: SPEC.md sections 2, 5, 6, 7, 8; AGENTS.md in full; src/lib/schema.ts (implementations enum is EXACTLY aria-apg, material, hig, fluent, carbon, polaris, radix, base-ui, shadcn); src/stage/choreography.ts; src/kit/*.css and src/kit/icons.ts.',
    STAGE_NEWS,
    `Category guidance: ${CATEGORY_GUIDANCE[category]}`,
    '',
    'Process terms ONE AT A TIME, each fully finished before the next:',
    `1. src/content/terms/<slug>.mdx: schema-valid frontmatter (status: published, category: ${category}, definition <= 200 chars, useWhen <= 90) + 2-4 paragraph article. NEVER use an em-dash anywhere. relations: author 2-4 edges against the discrimination test in SPEC 2.3 (contrastWith earns its place when someone describing this term could reach for the other term's word), symmetric edges declared on BOTH sides, every target an existing slug; prefer variantOf when the family already has a head term. tags: LEAVE EMPTY, a facet arrives complete or not at all (SPEC 2.5). If an alias collides with an existing term or its aliases, drop the alias and note it. Check collisions by searching src/content/terms/ for the alias text, not by guessing.`,
    '2. src/content/demos/<slug>/demo.ts: mount(root, clock) composing kit primitives (omit clock only if unused); inline styles for dimensions, one-off placement, and paint the brief legitimizes. data-subject on the narrowest element the term names (whole-scene only where genuinely so; note the decision), inset from frame edges; scenery in .sp-context; data-part on scripted targets; timers only from DemoClock; reserve revealed space; never measure synchronously after a style write (mount in the state you measure). The kit is FROZEN.',
    '3. src/content/demos/<slug>/choreography.ts: data-part selectors only; state-qualified asserts; give every assert room after a transition (the judge does not retry); the demo must answer synthesized events; only a term whose flip IS the term may toggle.',
    '',
    VISUAL_PASS,
    '',
    AUDIT_PASS,
    '',
    'Static checks are NOT your job: do not run bun validate, typecheck, tests, builds, or lint (the AUDIT PASS above is NOT one of these and is never optional: it is your own probe driving the page’s own stage, not a repo script); a dedicated verify phase runs them right after you finish. Be correspondingly careful with the schema and choreography as you write.',
    '',
    'TOKEN DISCIPLINE: one quick WebFetch per implementation URL; drop rows that do not verify; zero rows is legal. Nothing under e2e/. Keep returned notes to a sentence or two per term.',
    '',
    'Return via structured output: completed (slugs fully authored) and notes.',
  ].join('\n')
}

phase('Author')
// Derived from ROSTER, so a category the pool has emptied simply gets no agent instead of
// one told to author zero terms. The skill's roster step says to size the agent list this
// way; deriving it means nobody has to remember to.
const CATS = Object.keys(ROSTER).filter((c) => ROSTER[c].length > 0)
const results = await parallel(CATS.map((c) => () =>
  agent(batchPrompt(c), { label: `batch:${c}`, phase: 'Author', schema: BATCH_SUMMARY, model: 'opus', agentType: 'general-purpose' })))
const authored = results.filter(Boolean).flatMap((r) => r.completed)
const roster = Object.values(ROSTER).flat()
const missing = roster.filter((slug) => !authored.includes(slug))
log(`authored: ${authored.length}/${roster.length}`)
if (missing.length > 0) log(`NOT reported complete: ${missing.join(', ')}`)

phase('Verify')
const verifyPrompt = [
  `You are the verify gate for an authoring round in ${REPO}. Parallel agents just wrote src/content/terms/<slug>.mdx and src/content/demos/<slug>/{demo.ts,choreography.ts}. They ran NO static checks, so you run them all, once, and fix what fails.`,
  '',
  `THE ROUND'S ROSTER is these ${roster.length} slugs, and it is the contract: ${roster.join(', ')}.`,
  `Of those, ${authored.length} were reported finished: ${authored.join(', ') || 'none'}.`,
  missing.length > 0
    ? `${missing.length} were NOT reported finished: ${missing.join(', ')}. An agent that dies mid-response reports nothing, so a slug on this list may be absent, half-written, or in fact complete. Inventory each one (all three files present, status published, demo inline) and say in your notes which. THE ROUND IS INCOMPLETE and the main session has to finish it; your job is to describe the gap precisely, not to close it.`
    : 'Every roster slug was reported finished, so any absent file is a real surprise and belongs in your notes.',
  '',
  'DO NOT MINT A STUB FOR A ROSTER SLUG. When a term on the roster is missing and another term already declares a relation to it, SPEC 2.3 says create the stub, and here that rule is wrong: the roster says a full article was meant to exist, so a stub would silently convert a recoverable interruption into published debt under the gate\u2019s own signature. Leave the dangling relation dangling, let `bun validate` fail on it, and report it. A stub is only ever correct for a slug that is NOT on the roster (an author legitimately reached for a neighbour the round never planned), and even then say so explicitly in your notes.',
  '',
  'Run in order, fixing failures between runs until each is clean:',
  '1. `bun validate` (content gates: schema, em-dashes, bare domains in prose, unresolved prose links, alias collisions, data-subject, bare timers, stage-escape APIs, transitionend waits, ungated script animation, invalid selectors, assert presence). Fix errors in the new files directly. An alias collision is fixed by dropping the alias.',
  '2. `bun typecheck`',
  '3. `bun check` (Biome). THREE warnings are pre-existing and must be left alone: candidates.json file size, and two noDescendingSpecificity warnings in src/kit/motion.css. Fix only NEW problems, with `bun run fix` for mechanical ones.',
  '',
  'Rules: read AGENTS.md first; fix in the spirit of the file you are fixing (match its idiom, do not gut a demo to silence a type error); never touch src/kit/, src/stage/, e2e/, or terms that predate this round (EXCEPTION: leave the authors\u2019 own prose-only cross-link edits to pre-existing articles alone if they pass the gates). Do not run e2e, builds, or dev servers: the main session handles those, and the user is running the dev server on 4321 (leave it alone). Report any stray files authors left at the repo root (screenshots, probe scripts) rather than committing around them.',
  '',
  'Return structured output: clean (all three gates green), fixed (one short line per fix), incomplete (one line per roster slug that is absent or half-written, empty if none), notes.',
].join('\n')
const verify = await agent(verifyPrompt, { label: 'verify:gates', phase: 'Verify', schema: VERIFY_SUMMARY, model: 'opus', agentType: 'general-purpose' })

return {
  batches: CATS.map((c, i) => ({ category: c, ...(results[i] ?? { completed: [], notes: 'agent failed' }) })),
  roster: { planned: roster.length, reported: authored.length, missing },
  verify: verify ?? { clean: false, fixed: [], incomplete: missing, notes: 'verify agent failed' },
}
